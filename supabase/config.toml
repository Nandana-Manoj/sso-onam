# Supabase project config (minimal). Full config is generated by `npx supabase init`
# if/when you run it. For cloud, auth settings live in the dashboard.
project_id = "sso-onam"

[db]
major_version = 15

# NOTE: For the approved Mobile + Password auth WITHOUT SMS, configure in the
# Supabase dashboard (Authentication settings):
#   - Enable "Phone" provider, password-based sign-in
#   - DISABLE phone confirmations (no SMS provider needed in v1)
# (Mobile OTP is a later enhancement once DLT/WhatsApp is in place.)
